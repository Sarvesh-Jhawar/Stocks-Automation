name: Daily Stock Update

on:
  schedule:
    - cron: "*/5 * * * *"  # Runs every 5 minutes for testing
  workflow_dispatch:

jobs:
  update-stocks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: pip install yfinance pandas matplotlib

      - name: Run Stock Script
        run: python scripts/fetch_stocks.py

      - name: Commit and Push
        run: |
          git config --global user.name "Sarvesh-Jhawar"
          git config --global user.email "sarveshjhawar08@gmail.com"
          git add .
          git commit -m "Daily stock update $(date +'%Y-%m-%d')"
          git push
